# ─────────────────────────────────────────────────────────────────────────────
# 1. WEB FRAMEWORK & SERVER
# ─────────────────────────────────────────────────────────────────────────────
fastapi==0.111.0           # Asosiy FastAPI veb freymvork
uvicorn[standard]==0.29.0  # ASGI server (uvloop va httptools bilan tezkor ishlaydi)
python-multipart==0.0.9   # Form ma'lumotlari va fayllarni yuklash uchun zarur

# ─────────────────────────────────────────────────────────────────────────────
# 2. MA'LUMOTLAR BAZASI (Neon PostgreSQL & SQLAlchemy ORM)
# ─────────────────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30         # Zamonaviy asinxron ORM va SQL toolkit (v2)
psycopg2-binary==2.9.9    # PostgreSQL drayveri (Vercel muhitida kompilyatsiyasiz tez o'rnatiladi)

# ─────────────────────────────────────────────────────────────────────────────
# 3. VALIDATSIYA, AUTH VA MUHIT (Xavfsizlik)
# ─────────────────────────────────────────────────────────────────────────────
pydantic>=2.5.0            # Ma'lumotlar validatsiyasi (ImportError xatosini davolovchi V2 versiya)
email-validator==2.1.1     # Pydantic modellarida EmailStr ishlatish uchun majburiy kutubxona
passlib[bcrypt]==1.7.4     # Foydalanuvchi parollarini xavfsiz hashlash (bcrypt backend)
python-dotenv==1.0.1       # .env fayldan maxfiy kalitlarni xavfsiz yuklash uchun

# ─────────────────────────────────────────────────────────────────────────────
# 4. AI INTEGRATSIYA & TARMOQ (OpenRouter Fixed)
# ─────────────────────────────────────────────────────────────────────────────
openai>=1.30.0             # OpenRouter API bilan aloqa o'rnatuvchi OpenAI SDK
httpx==0.27.2              # Vercel'dagi proxies (500 Internal Server Error) xatosini mutlaqo tuzatuvchi barqaror versiya
